<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <version>1.0.0</version>
    <groupId>com.insightsoftware</groupId>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>connector-server-sharepoint</artifactId>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <inceptionYear>2026</inceptionYear>
        <currentYear>2026</currentYear>
        <copyrightOwner>insightsoftware</copyrightOwner>
        <!-- Versions -->
        <!--
          v1.1 dep bumps (H6 audit finding). Heritage versions came from the
          Zoomdata 2016/2017 template and carried CVE risk (guava 20 had
          CVE-2018-10237, logback 1.1.8 had multiple, org.json 20160810
          had CVE-2022-45688). slf4j + logback are now managed by the
          Spring Boot BOM (no explicit version). joda-time stays for v1.1
          (java.time migration deferred to v2 — bigger blast radius than
          warranted here).
        -->
        <connector-api.version>25.4.0</connector-api.version>
        <query.dsl.version>4.1.4</query.dsl.version>
        <lombok.version>1.18.34</lombok.version>
        <thrift.version>0.21.0</thrift.version>
        <dbcp2.version>2.13.0</dbcp2.version>
        <spring.boot.version>3.2.5</spring.boot.version>
        <json.version>20250517</json.version>
        <joda.time.version>2.13.1</joda.time.version>
        <commons.lang.version>3.18.0</commons.lang.version>
        <guava.version>33.4.0-jre</guava.version>
        <!-- SharePoint-specific -->
        <microsoft.graph.version>6.10.0</microsoft.graph.version>
        <azure.identity.version>1.12.0</azure.identity.version>
    </properties>

    <!-- edc-api installed locally from running EDC container (v25.4.0) -->

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!-- Microsoft Graph SDK (SharePoint, Lists, Excel Workbook, Drives) -->
        <dependency>
            <groupId>com.microsoft.graph</groupId>
            <artifactId>microsoft-graph</artifactId>
            <version>${microsoft.graph.version}</version>
        </dependency>

        <!-- Azure Identity (ClientSecretCredential for app-only auth) -->
        <dependency>
            <groupId>com.azure</groupId>
            <artifactId>azure-identity</artifactId>
            <version>${azure.identity.version}</version>
        </dependency>

        <!-- Kiota OkHttp middleware — required at compile scope so we can
             build a custom OkHttpClient with configurable timeouts (M1
             audit fix). Pulled transitively by microsoft-graph but only at
             runtime scope; we need it on the compile classpath too. -->
        <dependency>
            <groupId>com.microsoft.kiota</groupId>
            <artifactId>microsoft-kiota-http-okHttp</artifactId>
            <version>1.1.11</version>
        </dependency>
        <dependency>
            <groupId>com.microsoft.kiota</groupId>
            <artifactId>microsoft-kiota-authentication-azure</artifactId>
            <version>1.1.11</version>
        </dependency>

        <!-- Jackson CSV dataformat for INCLUDE_FILES CSV/TSV parsing (v1.1).
             jackson-databind/core are already on the classpath (transitive
             via kiota-serialization-json + spring-boot-starter-json at
             2.15.4); pin the CSV dataformat to the same version. -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-csv</artifactId>
            <version>2.15.4</version>
        </dependency>

        <!-- EDC API (Thrift generated classes) -->
        <dependency>
            <groupId>com.zoomdata</groupId>
            <artifactId>edc-api</artifactId>
            <version>${connector-api.version}</version>
        </dependency>

        <!-- Thrift -->
        <dependency>
            <groupId>org.apache.thrift</groupId>
            <artifactId>libthrift</artifactId>
            <version>${thrift.version}</version>
        </dependency>

        <!-- Logging — versions managed entirely by Spring Boot BOM (v1.1).
             Explicit versions on these in v1.0 pinned ancient 2016 jars and
             dragged known CVEs along. Let Spring Boot pick the matched set. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>

        <!-- Query DSL (needed by framework common/sql classes) -->
        <dependency>
            <groupId>com.querydsl</groupId>
            <artifactId>querydsl-core</artifactId>
            <version>${query.dsl.version}</version>
        </dependency>
        <dependency>
            <groupId>com.querydsl</groupId>
            <artifactId>querydsl-sql</artifactId>
            <version>${query.dsl.version}</version>
        </dependency>

        <!-- Connection Pool (needed by framework) -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-dbcp2</artifactId>
            <version>${dbcp2.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <version>${spring.boot.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-tomcat</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
            <version>${spring.boot.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jetty</artifactId>
            <version>${spring.boot.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.glassfish</groupId>
                    <artifactId>javax.el</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>${guava.version}</version>
        </dependency>
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>${json.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons.lang.version}</version>
        </dependency>
        <dependency>
            <groupId>joda-time</groupId>
            <artifactId>joda-time</artifactId>
            <version>${joda.time.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <!-- v1.1: SharePointSmokeTest lives in src/test/java now
                         (M4 audit fix). Test scope here includes both compile
                         and test classpaths, so the smoke test can use the
                         provider classes while staying out of the production
                         jar shipped to the pod. -->
                    <classpathScope>test</classpathScope>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <configuration>
                    <mainClass>com.zoomdata.connector.example.provider.sharepoint.SharePointConnectorMain</mainClass>
                    <jvmArguments>
                        -Duser.timezone=GMT
                    </jvmArguments>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                        <configuration>
                            <layout>ZIP</layout>
                            <classifier>exec</classifier>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
